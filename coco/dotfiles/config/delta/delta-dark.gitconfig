# Active delta theme (dark) — symlinked to theme-active.gitconfig by darkman.
# Included after [delta] in the generated git config, so these win.
[delta]
	light = false
	syntax-theme = "gruvbox-dark"
